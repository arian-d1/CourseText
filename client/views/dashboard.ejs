<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Dashboard</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 2rem; background: #fafafa; }
    .container { background: white; padding: 2rem; border-radius: 8px; max-width: 500px; margin: auto; }
    h1 { color: #222; }
    .user-info { margin-top: 1rem; }
    .field { margin-bottom: 0.5rem; }
    .label { font-weight: bold; }
    .password { font-family: monospace; background: #eee; padding: 0.2rem 0.5rem; border-radius: 4px; }
  </style>
</head>
<body>
  <div class="container">
    <h1>Your Dashboard</h1>

    <% if (user) { %>
      <div class="user-info">
        <div class="field">
          <span class="label">ID:</span> <%= user.id %>
        </div>
        <div class="field">
          <span class="label">Username:</span> <%= user.username %>
        </div>
        <div class="field">
          <span class="label">Password:</span> <span class="password"><%= user.password %></span>
        </div>
      </div>
    <% } else { %>
      <p>You are not logged in.</p>
    <% } %>
  </div>
</body>
</html>